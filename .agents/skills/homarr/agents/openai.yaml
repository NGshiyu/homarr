interface:
  display_name: "Homarr"
  short_description: "Repo-specific patterns and workflows for homarr"
  default_prompt: "Use the homarr repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true